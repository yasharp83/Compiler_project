import unittest
import runner

class Test(unittest.TestCase):
    def test_t1(self):
        score1, score2 = runner.run_and_evaluate('T1')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t2(self):
        score1, score2 = runner.run_and_evaluate('T2')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t3(self):
        score1, score2 = runner.run_and_evaluate('T3')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t4(self):
        score1, score2 = runner.run_and_evaluate('T4')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t5(self):
        score1, score2 = runner.run_and_evaluate('T5')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t6(self):
        score1, score2 = runner.run_and_evaluate('T6')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t7(self):
        score1, score2 = runner.run_and_evaluate('T7')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t8(self):
        score1, score2 = runner.run_and_evaluate('T8')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t9(self):
        score1, score2 = runner.run_and_evaluate('T9')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_t10(self):
        score1, score2 = runner.run_and_evaluate('T10')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_s1(self):
        score1, score2 = runner.run_and_evaluate('S1')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_s2(self):
        score1, score2 = runner.run_and_evaluate('S2')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_s3(self):
        score1, score2 = runner.run_and_evaluate('S3')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_s4(self):
        score1, score2 = runner.run_and_evaluate('S4')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_r1(self):
        score1, score2 = runner.run_and_evaluate('R1')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_r2(self):
        score1, score2 = runner.run_and_evaluate('R2')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_r3(self):
        score1, score2 = runner.run_and_evaluate('R3')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)

    def test_r4(self):
        score1, score2 = runner.run_and_evaluate('R4')
        self.assertEqual(score1, 0)
        self.assertEqual(score2, 0)
