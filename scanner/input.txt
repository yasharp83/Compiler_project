/* A simple test program */

void main(void) {
  int x;
  int y;
  
  x = 10;
  y = 20;
  
  if (x < y) {
    output(x + y);
  } else {
    output(x - y);
  }
  
  while (x > 0) {
    x = x - 1;
  }
  
  return;
} 